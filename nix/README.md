# workarounds for home-manager on arch


## link icons

```sh
ln -s ~/.nix-profile/share/icons ~/.icons
```

## link themes

```sh
ln -s ~/.nix-profile/share/themes ~/.themes
```

## link nix apps to rofi

```sh
ln -s ~/.nix-profile/share/applications ~/.local/share/applications
```
